# === Postgres (used by docker-compose) ===
POSTGRES_DB=journaling_app
POSTGRES_USER=journal_user
POSTGRES_PASSWORD=journal_pass

# === Backend (FastAPI) ===
# Compose points the backend at the "postgres" service on the Docker network:
DATABASE_URL=postgresql+psycopg://journal_user:journal_pass@postgres:5432/journaling_app

# CORS for local dev
CORS_ALLOW_ORIGINS=http://localhost:5173

# === Optional AI (safe placeholders) ===
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.1-8b-instant

# === Demo login (matches db/init.sql seed) ===
DEMO_USER=user1
DEMO_PASSWORD=journal1

# === Frontend build-time (used by Dockerfile.frontend) ===
PUBLIC_API_URL=http://localhost:8000
VITE_DEMO_TOKEN=super-secret-demo-token